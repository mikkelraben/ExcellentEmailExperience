<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ExcellentEmailExperience.Views.Email"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ExcellentEmailExperience.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Height="60">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="From: Totally-Not-A-Scam Car Warranty Services, Inc." IsTextSelectionEnabled="True" Margin="12,12" Name="From" Grid.Column="0"/>
            
        </Grid>
        <ScrollView Grid.Row="1" x:Name="ScrollView" VerticalScrollBarVisibility="Auto">
            <TextBlock TextWrapping="Wrap" IsTextSelectionEnabled="True" Margin="12,12" Name="MailContent" Visibility="Collapsed">
                No mail content.
            </TextBlock>
        </ScrollView>
        <Grid Grid.Row="1"  Name="EmptyMail">
            <TextBlock Text="No mail selected." IsTextSelectionEnabled="True" Margin="12,12" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>
        <WebView2 Grid.Row="1" x:Name="HTMLViewer" Visibility="Collapsed" CanGoBack="False" CanGoForward="False" CanDrag="False" DefaultBackgroundColor="Transparent" Source="about:blank"/>
    </Grid>
</Page>
